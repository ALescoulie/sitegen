<!DOCTYPE html>

<head>
    {{ header }}
</head>

<body>
    {{ navbar }}

    <div class="main">
        <div class="post-header">
            <h1>{{ post_title }}</h1>
            <h2>{{ post_author }}</h2>
            <h3>{{ post_date }}</h3>
        </div>

        <div class="blog_text">
            {{ post_html }}
        </div>
    </div>
</body>
